var i = 0;
do {
    i++;
    document.write(`The number is ${i}.<br>`)
} while (i < 10);
// This will iterate through the loop starting at 0 and working its way up until it gets to 10 which is when it will stop looping because 10 is NOT less than 10.